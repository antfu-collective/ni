name: Test

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
    - uses: actions/checkout@v1

    - name: install
      run: yarn global add @antfu/ni

    - run: ni

    - run: nr lint

    - run: nr test
